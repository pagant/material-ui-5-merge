import React from 'react';
import SideNavigationAction from '../SideNavigationAction';
import Icon from '../../Icon/Icon';

export default (
  <SideNavigationAction uxpId="side-navigation-action-1" label="Recents" showLabel icon={<Icon uxpId="icons-1">restore</Icon>} />
);